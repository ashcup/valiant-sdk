-- Require the `application` and `console` modules.
local application   = require "valiant.application"
local console       = require "valiant.console"

-- Log a message.
console.Log("Hello, Valiant!")

-- Exit the application.
application.Exit(0)
